# Быстрый старт - Загрузка фото в Swagger

## Что было исправлено
✅ Добавлена поддержка multipart/form-data для загрузки файлов  
✅ Настроен Swagger UI для отображения полей загрузки файлов  
✅ Добавлены parser_classes во все views с загрузкой фото  
✅ Обновлены настройки drf-spectacular  

## Как загрузить фото в Swagger

### 1. Запустите сервер
```bash
cd server
python manage.py runserver
```

### 2. Откройте Swagger
http://localhost:8000/docs/

### 3. Для тестирования эндпоинтов с фото:

#### Регистрация (`/api/auth/register/`)
```
1. Нажмите "Try it out"
2. Заполните поля:
   - username: test_user
   - password: password123
   - name: Test User
3. В поле "photo" нажмите "Choose File" → выберите фото
4. Execute
```

#### Photo Login (`/api/auth/photo-login/`)
```
1. Сначала получите токен через /api/auth/login/
2. Нажмите "Authorize" вверху → введите "Bearer <ваш_токен>"
3. Перейдите к /api/auth/photo-login/
4. "Try it out" → выберите фото → Execute
```

#### Feedback Photo (`/api/feedback/photo/`)
```
1. Авторизуйтесь (Authorize → Bearer <токен>)
2. Перейдите к /api/feedback/photo/
3. "Try it out"
4. В поле "file" выберите фото с лицом
5. Execute
```

## Установка обновлений
```bash
pip install -r requirements.txt
```

## Теперь в Swagger вы увидите
- Кнопки "Choose File" для загрузки фото
- Правильное отображение multipart/form-data
- Возможность тестировать все эндпоинты с загрузкой файлов

Готово! 🎉
